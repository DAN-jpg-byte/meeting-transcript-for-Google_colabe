# ロードマップ v2.0

作成日: 2026-04-19

---

## 全体の流れ

```
Step 1  変更なし（uv インストール）
Step 2  変更なし（全認証）
Step 3  ★変更あり：WhisperX から話者分離を削除
Step 4a ★新規：Gemini で話者分離のみ（1万文字ずつ）
Step 4b ★変更あり：Gemini で誤字補正・整形のみ（1万文字ずつ）
Step 5  変更なし（Notion 出力）
Step 6  変更なし（後片付け）
```

---

## 作業ステップ

### - [ ] Step A：WhisperX の話者分離を削除（cell-9 を削除）

**変更ファイル：** `notebook.ipynb`

**やること：**
- `cell-9`（DiarizationPipeline の処理）をまるごと削除
- `cell-10` の出力形式を変更
  - 変更前：`[SPEAKER_00] テキスト`
  - 変更後：ただのテキスト（話者ラベルなし）

---

### - [ ] Step B：Gemini 話者分離プロンプトを新規作成（cell-14 を置き換え）

**変更ファイル：** `notebook.ipynb`

**やること：**
- 既存の cell-14（話者特定）を改修
- 入力：文字起こしテキスト（1万文字） ＋ 参加者情報
- プロンプト：「話者を特定して `名前：発言内容` 形式にする」だけに集中
- 誤字補正・整形は一切させない

---

### - [ ] Step C：Gemini 誤字補正・整形プロンプトを分離（cell-16 を改修）

**変更ファイル：** `notebook.ipynb`

**やること：**
- Step B の出力を受け取る
- プロンプト：「誤字補正・整形」だけに集中
- 話者名は絶対に変えない
- 1万文字ずつ分割して処理

---

### - [ ] Step D：動作確認

**やること：**
- 実際の音声ファイルで全ステップ通しテスト
- 話者分離の精度が改善されているか確認
- Notion に正しく出力されるか確認

---

### - [ ] Step E：コミット・タグ付け

```bash
git add notebook.ipynb
git commit -m "feat: 話者分離と整形を別プロンプトに分離（v2.0）"
git tag -a v2.0.0 -m "v2.0.0: 話者分離精度改善"
git push origin main
git push origin v2.0.0
```

---

## 注意点

- API リクエスト数が増えるため、Gemini の無料枠（RPD: 20）に注意
- 長時間の会議音声では Step 4a・4b 合わせてリクエスト数が増える
- テスト時は短い音声ファイル（5〜10分）で確認推奨
